import { KNotationFormatPipe } from './k-notation-format.pipe';

describe('KNotationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new KNotationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
